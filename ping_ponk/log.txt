blob
	Header    - int 0xABC456	// 4 bytes
	Player[4] - int x, y, w, h;	// 100 bytes
		    float vx, vy;
		    int active;
	Ball	  - int x, y, r;	// 20 bytes
		    float vx, vy;
	Status	  - int status;		// 4 bytes


0427
pingo.c
	pureevil.ttf is stupid cause wtf it makes roman numbs.
	code struct is not very good todo:	clean up & format it. 
	cmake will not work cause the texture and ttf is not there and cmaketxt is
	not updated. btw compling and running from inside the pingo/ will work.
	cmd:	gcc pingo.c -Wall -lSDL3 -lSDL3_image -lSDL3_ttf
	run:	./a.out
	over over